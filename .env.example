# Read by Docker Compose only (Part 2 of the README).
# These values have no effect when you run the app locally with Maven and Vite -
# local database settings live in api/local.properties instead.
#
# Copy this file to .env and choose your own credentials.

MYSQL_ROOT_PASSWORD=
MYSQL_DATABASE=
MYSQL_USER=
MYSQL_PASSWORD=
